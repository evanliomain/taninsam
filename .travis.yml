language: node_js
branches:
  only:
    - master
    - /^greenkeeper/.*$/
cache:
  yarn: true
  directories:
    - node_modules
notifications:
  email: false
node_js:
  - node
script:
  - npm run lint
  - npm run test:ci
  - npm run build
  - npm run stryker:ci
after_success:
  - npm run report-coverage
  - npm run deploy-docs
  - npm run semantic-release
